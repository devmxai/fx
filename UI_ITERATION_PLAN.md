# FX Project UI Iteration Plan

## Scope

- العمل الحالي يقتصر على `UI` فقط.
- نحافظ على `structure`, `source code contracts`, وامتدادات المشروع الحالية كما هي.
- لا نغير `hooks`, `libs`, `db`, أو منطق التصدير إلا إذا أصبح ذلك ضروريًا جدًا وبعد مراجعة منفصلة.

## Guardrails

- كل جولة تعديل تكون صغيرة ومحصورة في جزء واجهة واحد.
- لا نجمع أكثر من هدف UI كبير في نفس الجولة.
- قبل أي تعديل: نحدد الجزء المستهدف، الهدف البصري، ومعيار القبول.
- بعد أي تعديل: نراجع `desktop`, `tablet`, `mobile` ثم ننتقل للخطوة التالية.
- إذا ظهر احتياج لتغيير بنيوي، يتم إيقاف الجولة ورفعه كبند مستقل بدل خلطه مع تعديل UI.

## Iteration Loop

1. تحديد جزء واحد فقط من الواجهة.
2. فحص سريع للملفات المرتبطة به.
3. تنفيذ تعديل UI صغير وآمن.
4. تشغيل المعاينة ومراجعة الشاشات الثلاث.
5. تثبيت النتيجة ثم الانتقال للجزء التالي.

## UI Sequence

1. Shell responsiveness
   - تنظيم layout العام لسطح المكتب والتابلت والموبايل بدون كسر الهيكل الحالي.
2. Desktop polish
   - تحسين المسافات، المحاذاة، والانقسام البصري مع الحفاظ على تجربة الديسكتوب الحالية.
3. Tablet mode
   - ترتيب الألواح بشكل أكثر توازنًا واستغلال أفضل للمساحة المتوسطة.
4. Mobile mode
   - تحويل الألواح الجانبية إلى patterns مناسبة مثل drawer, bottom sheet, tabs.
5. Control pass
   - تكبير touch targets وتحسين أزرار التحكم والتنقل داخل الواجهة.
6. Visual polish
   - تحسين التناسق، التفاصيل، والحالات الفارغة بدون توسيع نطاق العمل.

## Review Checklist Per Step

- هل الجزء المستهدف فقط هو الذي تغيّر؟
- هل بقيت تجربة الديسكتوب سليمة؟
- هل أصبح العرض أوضح على التابلت؟
- هل صار الاستخدام ممكنًا على الموبايل بدون قص أو اختناق؟
- هل ما زال المشروع يعمل على السيرفر المحلي بدون كسر؟

## Current Rule

- نبدأ من `layout shell` فقط.
- لا نلمس الـ timeline logic أو business logic في هذه المرحلة.
